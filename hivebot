#!/bin/bash
# Wrapper for layer management CLI

#SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
#exec python3 "$SCRIPT_DIR/scripts/layer_cli.py" "$@"

# HiveBot CLI wrapper – delegates to layer_cli.py
python /app/scripts/layer_cli.py "$@"
